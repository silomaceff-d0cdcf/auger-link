<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pre-Compose splash theme; applied to the window before setContent runs. -->
    <style name="Theme.AugerLink" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/barn_dark</item>
        <item name="android:statusBarColor">@color/barn_dark</item>
        <item name="android:navigationBarColor">@color/barn_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
